% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlb_highlights.R
\name{mlb_fetch_highlights}
\alias{mlb_fetch_highlights}
\title{Fetch MLB highlight video URLs for a team, filtered by player and (optionally) play description}
\usage{
mlb_fetch_highlights(
  start_date,
  end_date,
  team,
  player = NULL,
  play_description = NULL,
  include_hls = FALSE,
  keep_ids = FALSE,
  search_type = c("contains", "exact", "regex"),
  parallel = NULL,
  progress = TRUE,
  timeout = 30
)
}
\arguments{
\item{start_date}{Character or Date, "YYYY-MM-DD" (inclusive).}

\item{end_date}{Character or Date, "YYYY-MM-DD" (inclusive).}

\item{team}{Character. Team name (partial match, case-insensitive).}

\item{player}{Character or NULL. Player name (partial, case-insensitive). If NULL, returns all team highlights.}

\item{play_description}{Character, optional. Extra keyword(s) to match in title/description.}

\item{include_hls}{Logical. Keep HLS (\code{.m3u8}) rows too? Default \code{FALSE} (MP4 only).}

\item{keep_ids}{Logical. Keep \code{game_pk}/\code{item_id} columns? Default \code{FALSE}.}

\item{search_type}{Character. One of "contains" (default), "exact", or "regex" for player/description matching.}

\item{parallel}{Logical. Use parallel processing for multiple games? Default \code{NULL} (auto-detect).}

\item{progress}{Logical. Show progress indicators? Default \code{TRUE}.}

\item{timeout}{Numeric. Timeout in seconds for API calls. Default \code{30}.}
}
\value{
\code{data.frame} with columns:
\code{game_date}, \code{home_team}, \code{away_team}, \code{title}, \code{description}, \code{published},
\code{playback_name}, \code{url} (plus ids if \code{keep_ids = TRUE}).
Includes attribute \code{query_info} with search parameters and results summary.
}
\description{
Queries MLB Stats API for games in a date range (inclusive), filters to a team,
extracts highlight items, and returns a tidy data.frame of \strong{playback URLs}.
Duplicates are removed and MP4s are preferred; HLS can be kept optionally.
}
