% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mlb_highlights.R
\name{mlb_browse_highlights}
\alias{mlb_browse_highlights}
\title{Interactively view or download highlight videos from a fetched table}
\usage{
mlb_browse_highlights(
  highlights_df,
  action = c("view", "download"),
  rows = NULL,
  destdir = NULL,
  max_display = 20
)
}
\arguments{
\item{highlights_df}{Data frame from \code{mlb_fetch_highlights()}.}

\item{action}{"view" or "download". If missing, a menu is shown.}

\item{rows}{Integer indices to act on. If \code{NULL}, a prompt is shown.}

\item{destdir}{Save directory. Defaults to \code{./videos} (created if needed).}

\item{max_display}{Maximum number of rows to display. Default 20.}
}
\value{
Invisibly, the subset acted upon (with the same columns as input).
}
\description{
Prints a clean, indexed list (no URLs), then lets the user select rows by index
to either open in a browser (MP4 works; HLS may require Safari/VLC) or download
to \code{./videos} by default. HLS rows are automatically converted to MP4 using
\code{ffmpeg} if available.
}
